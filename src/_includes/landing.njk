{%- set d = rancho if data == "rancho" else irvine -%}
{%- set location = d.location -%}
{%- set phone = d.phone -%}
{%- set phoneTel = d.phoneTel -%}
{%- set address = d.address -%}
{%- set city = d.city -%}
{%- set hours = d.hours -%}
{%- set rating = d.rating -%}
{%- set reviewCount = d.reviewCount -%}
{%- set heroHeadline = d.heroHeadline -%}
{%- set heroSubheadline = d.heroSubheadline -%}
{%- set serviceAreas = d.serviceAreas -%}
{%- set serviceAreasLong = d.serviceAreasLong -%}
{%- set portfolio = d.portfolio -%}
{%- set testimonials = d.testimonials -%}
{%- set faq = d.faq -%}
{%- set heroImage = d.heroImage -%}
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Custom Window Wraps & Storefront Graphics in {{ location }} | AlphaGraphics</title>
  <meta name="description" content="Professional window wraps & storefront graphics in {{ location }}. Free same-day quotes, precision installation, full warranty. Call or Text {{ phone }} today!">
  <meta name="robots" content="index, follow">
  <link rel="canonical" href="https://go.alphagraphics{{ data }}.com/">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Roboto+Slab:wght@400;700&family=Roboto:wght@300;400;500;700&display=swap" rel="stylesheet">
  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      theme: {
        extend: {
          colors: {
            'ag-red': '#DA291C',
